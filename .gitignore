## Android / Gradle build output
*.iml
.gradle/
build/

## Khóa ký release thật — PHẢI giữ ngoài repo (mất là không bao giờ cập nhật
## được app dưới cùng identity nữa, lộ ra thì ai cũng ký giả được bản cập
## nhật). Mật khẩu nằm ở local.properties (đã ignore riêng dòng dưới).
key-store.jks
key-store.jks.old-nopassword
captures/
.externalNativeBuild/
.cxx/
local.properties
.idea/
*.apk
*.aab
*.ap_
*.dex

## NDK / native toolchain download cache (nếu build script tự tải về máy)
.ndk/

## Runtime JDK/JRE build cho ARM (build tay/tải sẵn, không commit binary lớn)
runtime/*.tar.xz
runtime/*.zip

## Tài liệu không liên quan tới project này (slide môn học đại học của
## người dùng, gửi nhầm lúc trao đổi — xem docs/ để biết tài liệu THẬT
## đang dùng cho project). Giữ lại trên máy cục bộ, không commit.
1.LTWEB.pdf

## macOS / Windows
.DS_Store
Thumbs.db

## ------ Kế thừa từ upstream Fold Craft Launcher ------
*.log
release
output.json
android-ndk-r26d/
.trae/
google-services.json
*.hprof
FCL/src/main/java/com/mio/Main.kt
/LWJGL-Pojav/bin
LWJGL-Pojav/compileOnly/jspecify-1.0.0.jar
AGENTS.md
/.zcode/
